-- Age of Heroes - AutoClick Toque UltraRápido (Mobile)
-- Hecho por Adrian (MACRO UI)

-- CONFIGURACIÓN
local clicksPorToque = 7        -- MÁS clics por toque
local delayEntreClicks = 0.015  -- Delay mínimo seguro (15ms aprox)

-- FUNCIONES
local function simularClick()
    local virtualUser = game:GetService("VirtualUser")
    virtualUser:Button1Down(Vector2.new(), workspace.CurrentCamera.CFrame)
    wait()
    virtualUser:Button1Up(Vector2.new(), workspace.CurrentCamera.CFrame)
end

-- ACTIVADOR DE TOQUE (MÓVIL)
game:GetService("UserInputService").TouchStarted:Connect(function()
    for i = 1, clicksPorToque do
        task.spawn(simularClick)
        task.wait(delayEntreClicks)
    end
end)

-- NOTIFICACIÓN
pcall(function()
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "AutoClick Ultra",
        Text = "x" .. clicksPorToque .. " por toque (Rápido)",
        Duration = 2
    })
end)
